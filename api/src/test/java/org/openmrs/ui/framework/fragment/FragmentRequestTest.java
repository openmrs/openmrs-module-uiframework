package org.openmrs.ui.framework.fragment;


import org.junit.Assert;
import org.junit.Ignore;
import org.junit.Test;

@Ignore
public class FragmentRequestTest {
	
	/**
	 * @see FragmentRequest#FragmentRequest(String)
	 * @verifies handle an id of provider:name
	 */
	/*
	@Test
	public void FragmentRequest_shouldHandleAnIdOfProvidername() throws Exception {
		FragmentRequest fragmentRequest = new FragmentRequest("mymoduleid:decorator/name");
		Assert.assertEquals("mymoduleid", fragmentRequest.getProviderName());
		Assert.assertEquals("decorator/name", fragmentRequest.getFragmentId());
	}
	*/
	
}